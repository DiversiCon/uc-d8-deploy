{#
/**
 * alert Twig Partial
 *
 */
#}

<uc-alert
  heading="{{ content.heading|convertQuotes }}"
  description="{{ content.description|convertQuotes }}"
  url="{{ content.url }}"
  url-text="{{ content.urlText }}"
  url-target="{{ content.urlTarget }}">
</uc-alert>
