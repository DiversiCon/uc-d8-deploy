{#
/**
 * photo Twig Partial
 *
 */
#}

<uc-photo
  img-full="{{ photo.imgFull }}"
  img-large="{{ photo.imgLarge }}"
  img-medium="{{ photo.imgMedium }}"
  img-small="{{ photo.imgSmall }}"
  description="{{ photo.description }}"
  credit="{{ photo.credit }}"
  alt="{{ photo.alt }}">
</uc-photo>
